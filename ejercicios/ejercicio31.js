function ejercicio31(){
    let r = parseInt(prompt("Ingresa el valor de r"));
    let c = parseInt(prompt("Ingresa el valor de c"));
    let x = parseInt(prompt("Ingresa el valor de x"));
    let y = parseInt(prompt("Ingresa el valor de y"));

    let resultado = r * c ((x* 2) -(x * 1) * (x* 2) -(x * 1)) + ((y*2)- (y*1) * (y*2)- (y*1));
    alert(`El resultado de la ecuacion es: ${resultado}`);
}