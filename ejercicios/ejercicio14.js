function ejercicio12() {
  let base = prompt("Ingresa la base del triangulo");
  let altura = prompt("Ingresa la altura del triangulo");

  let resultado = (base * altura) / 2;
  alert(`La base del triangulo es: ${resultado}`)
}